<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Healthme" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="application-name" content="Healthme">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="data:image/svg+xml,<svg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 100 100'><text y='.9em' font-size='90'>🍎</text></svg>">
    <link rel="apple-touch-icon" href="/icon.svg">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body data-controller="biometric-lock">
    <% if signed_in? %>
      <nav class="app-nav">
        <span class="nav-user"><%= current_user.display_name.presence || current_user.short_npub %></span>
        <div class="nav-links">
          <%= link_to "Home", root_path, class: "btn btn-sm btn-outline" %>
          <%= link_to "Profile", profile_path, class: "btn btn-sm btn-outline" %>
          <%= button_to "Sign Out", session_path, method: :delete, class: "btn btn-sm btn-outline" %>
        </div>
      </nav>
    <% end %>

    <main>
      <%= yield %>
    </main>

    <%# Biometric lock overlay — shown by Stimulus on native when Face ID/Touch ID is enabled %>
    <div class="biometric-overlay" data-biometric-lock-target="overlay" style="display: none;">
      <div class="biometric-overlay-content">
        <div class="biometric-icon">🍎</div>
        <h2>HealthMe is Locked</h2>
        <p>Authenticate to continue</p>
        <button class="btn btn-primary" data-biometric-lock-target="retryButton" data-action="biometric-lock#retry" style="display: none;">
          Try Again
        </button>
      </div>
    </div>
  </body>
</html>
